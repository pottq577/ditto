spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    # show-sql: true
    properties:
      hibernate:
        # format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

rapidapi:
  background-removal:
    key: 08163221acmsh9d44e2faab8fbd5p19ecf9jsnfa1e46c83483
    host: background-removal-ai.p.rapidapi.com
